[build]
  builder = "NIXPACKS"

[[services]]
  name = "mbom-backend"
  port = 8080
  healthCheckPath = "/api/health"
  startCommand = "cd backend && npm install && npx prisma generate && npm run build && cd backend && npm start"

  [services.environment]
    NODE_ENV = "production"
    PORT = "8080"
